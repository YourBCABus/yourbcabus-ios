query GetSchools {
    schools {
        id
        name
        readable
    }
}

query GetBuses($schoolID: ID!) {
    school(id: $schoolID) {
        name
        buses {
            id
            name
        }
    }
}
